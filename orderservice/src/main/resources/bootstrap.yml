spring:
  application:
    name: order-service
  cloud:
    config:
      name: order-service
      profile: default
      uri: http://localhost:8000/

server:
  port: 9002

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761