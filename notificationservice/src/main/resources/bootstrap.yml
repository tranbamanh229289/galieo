spring:
  application:
    name: notification-service
  cloud:
    config:
      name: notification-service
      profile: default
      uri: http://localhost:8000

server:
  port: 9004

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761        # registry discovery server
